{{!-- <article class="post">
  <div class="title">
    {{debug this}}
    <h3>{{title}}</h3>
    <p>{{post_body}}</p>
    <p>by {{user.user_name}}</p>
  </div>
  <div class="meta">
    <a href="/posts/{{id}}">{{comments.length}}
      {{format_plural "comment" comments.length}}</a>
  </div>
</article> --}}

<h5 id="card-post-title" class="card-title">{{title}}</h5>
<h6 class="card-subtitle mb-2 text-muted">by {{user.user_name}}</h6>
<p class="card-text">{{post_body}}</p>
<button type="button" class="btn btn-outline-secondary" onclick="location.href='/posts/{{id}}'">{{comments.length}}
  {{format_plural "comment" comments.length}}</button>

<button type="button" class="btn btn-outline-secondary" onclick="location.href='/edit-post/{{id}}'">
  Edit Post
</button>

{{!-- <!-- Modal -->
<div class="modal fade" id="editRequestModal{{id}}" tabindex="-1" aria-labelledby="editRequestModalLabel{{id}}" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <form class="justify-content-start" id='editRequest'>
        <div class="modal-header">
          <h5 class="modal-title" id="editRequestModalLabel{{id}}">Edit post!</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <h2><label for="post-title"></label>Title</label></h2>
          <input name="post-title" type="text" value="{{title}}" />
          <h2><label for="post-body">Content</label></h2>
          <input name="post-body" type="text" value="{{post_body}}" />
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
          <button type="submit" class="btn btn-primary">Save changes</button>
        </div>
      </form>
    </div>
  </div>
</div> --}}

<script src="/javascript/edit-post.js"></script>